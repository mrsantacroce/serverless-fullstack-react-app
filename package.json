{
  "name": "serverless-fullstack-react-app",
  "version": "1.0.0",
  "description": "Serverless fullstack React application",
  "scripts": {
    "validate": "serverless print",
    "deploy": "serverless deploy",
    "remove": "serverless remove"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "serverless": "^4.0.0"
  }
}